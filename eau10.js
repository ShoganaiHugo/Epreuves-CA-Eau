function minToMax(str) {
  // On gère les erreurs de paramètres
  if (str.length !== 2 || !isNaN(str) === true) {
    return 'error';

  } else {


  }
}


console.log(minToMax(process.argv.slice(2)));